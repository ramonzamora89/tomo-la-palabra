# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Vercel CLI local link/config (contains org/project IDs, not secret but local-only)
.vercel/

# Env / secrets
.env
.env.local
.env*.local

# Editor / OS
.DS_Store

# Brand source assets — large binaries, not needed to build/run the site
# Material de video crudo (solo la carpeta de la raíz). Anclada con `/`
# porque este repo vive en un volumen exFAT y git corre con
# core.ignorecase=true: sin el ancla, `VIDEOS/` también capturaba
# `app/videos/`, que quedó fuera del repo sin que nadie lo notara.
/VIDEOS/
USO DE REDES/
LOGO/*.ai
TOMO LA PALABRA_BRANDBOOK 2025.pdf

# Pipeline runtime artifacts
pipeline/tmp/
*.log
*.tsbuildinfo
.vercel
.env*

# Google credential files downloaded straight into the project by mistake —
# never commit these. The real values live in .env.local / GitHub Secrets.
client_secret_*.json
*-33ed9348189c.json

# Business presentation deck — not site source, kept out of this repo
presentacion-flujo/

# Local Claude Code session state
.claude/
