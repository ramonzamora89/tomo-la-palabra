# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Vercel CLI local link/config (contains org/project IDs, not secret but local-only)
.vercel/

# Env / secrets
.env
.env.local
.env*.local

# Editor / OS
.DS_Store

# Brand source assets — large binaries, not needed to build/run the site
VIDEOS/
USO DE REDES/
LOGO/*.ai
TOMO LA PALABRA_BRANDBOOK 2025.pdf

# Pipeline runtime artifacts
pipeline/tmp/
*.log
.vercel
.env*
